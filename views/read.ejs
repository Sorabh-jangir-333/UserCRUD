<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 p-10 text-white">
    <div class="w-full h-full">
        <a href="/" class="text-blue-500 hover:text-gray-300">Back To Home</a>
        <h1 class="text-3xl tracking-tighter mb-3">All Users</h1>

        <div class="users flex gap-6 flex-wrap">
            <% if (users.length > 0) { %>
                <% users.forEach(function(user) { %>
                    <div class="user w-64 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-72 rounded-lg bg-zinc-700 overflow-hidden">
                            <img src="<%= user.image %>" alt="User Image" class="w-full h-full object-cover object-top">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-3 mt-4">
                            <a class="text-zinc-200 hover:text-blue-400" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-500 hover:text-red-700" href="/delete/<%= user._id %>">Delete</a>
                        </div>
                    </div>
                <% }); %>  <!-- Fixed the loop closure here -->
            <% } else { %>
                <h3 class="text-zinc-500">No User Yet</h3>
            <% } %>
        </div>
    </div>
</body>
</html>
